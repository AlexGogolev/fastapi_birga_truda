version: 3
services:
  db:
    image: postgres:14
    ports:
      - 32700:6432
    volumes:
      - ./data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: root
      POSTGRES_PASSWORD: root
      POSTGRES_DB: birga_truda